/** Bare /console/ebm index — the merged Finance & EBM sidebar item links
 *  here. Shows the first sub-nav section (Revenue & Billing) so landing on
 *  the hub isn't empty; the sub-nav (see layout.tsx) highlights it as active
 *  via the same segment-fallback SectionShell uses for every other section. */
export { default } from './revenue/page';
