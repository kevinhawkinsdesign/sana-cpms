// This route's implementation was disabled (see git history) — shift swaps
// live under /dashboard/operator/shifts/swaps instead. Kept as a minimal
// valid page (rather than a file with no default export at all) since an
// empty module here was confusing Next's static-export prerendering.
export default function ShiftSwapsPage() {
  return null;
}
